<%- include('../../partials/header'); %>

<!-- BODY HERE -->

          <div class="col-lg-10 col-md-9">


            <div class="panel panel-default">
              <div class="panel-body">

                      <% groups.forEach(function(group) { %>
                      <h2>  <%= group.subject_name %> -
                        <%= group.group_name %></h2>
                      <%= group.group_start_date %> - <%= group.group_end_date %></span>
                              <p class="summary"><%= group.group_description %></p>
                            </div>
                          </div>
                      <% }); %>



              </div>
    <!-- BODY END -->
<%- include('../../partials/footer'); %>
